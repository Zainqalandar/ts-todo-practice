installation

Runtime dependencies:
npm install express mongoose dotenv

Dev dependencies (TypeScript tools):
npm install -D typescript ts-node-dev @types/node @types/express

3. TypeScript init karo
npx tsc --init

4. tsconfig.json me thori settings change karo

{
  "target": "ES6",
  "module": "commonjs",
  "rootDir": "./src",
  "outDir": "./dist",
  "strict": true,
  "esModuleInterop": true,
  "verbatimModuleSyntax": false
}

🧠 Samajhne wali important baat

👉 Model → DB structure define karta hai
👉 Controller → logic handle karta hai
👉 Routes → kis URL pe konsa function chalega
👉 App → middleware + routes mount
👉 Server → DB connect + server start

npx ts-node-dev src/index.ts